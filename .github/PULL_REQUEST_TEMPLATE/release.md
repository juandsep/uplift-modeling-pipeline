<!-- Release: `dev` into `main`. Merging deploys to Cloud Run after approval. -->

## Changes in this release

<!-- One line per merged branch, e.g. `feat/gcp-deploy`: deploy the API to Cloud Run. -->

-

## Deploy notes

<!-- New env vars, secrets, GCP resources, or MODEL_VERSION change. Write "None" if nothing. -->

## Checklist

- [ ] CI is green on `dev`
- [ ] `MODEL_VERSION` in the repository variables points to the model to serve
- [ ] Rollback: previous Cloud Run revision is known
